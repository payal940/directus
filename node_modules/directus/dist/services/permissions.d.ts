import { AbstractServiceOptions, PermissionsAction } from '../types';
import { ItemsService } from '../services/items';
export declare class PermissionsService extends ItemsService {
    constructor(options: AbstractServiceOptions);
    getAllowedCollections(role: string | null, action: PermissionsAction): Promise<any[]>;
    getAllowedFields(role: string | null, action: PermissionsAction, collection?: string): Promise<Record<string, string[]>>;
}
