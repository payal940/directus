import { QueryBuilder } from 'knex';
import { Query, Filter, SchemaOverview } from '../types';
import Knex from 'knex';
export default function applyQuery(knex: Knex, collection: string, dbQuery: QueryBuilder, query: Query, schema: SchemaOverview): Promise<void>;
export declare function applyFilter(knex: Knex, rootQuery: QueryBuilder, rootFilter: Filter, collection: string): Promise<void>;
